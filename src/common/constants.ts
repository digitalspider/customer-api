export const AWS = {
  REGIONS: {
    AWS_REGION_APSE2: 'ap-southeast-2',
  },
};

export const JWT_SECRET_NAME = 'jwt-secret';

export enum HTTP_METHOD {
  GET = 'GET',
  POST = 'POST',
  PUT = 'PUT',
  PATCH = 'PATCH',
  DELETE = 'DELETE',
  HEAD = 'HEAD',
  OPTIONS = 'OPTIONS',
}
